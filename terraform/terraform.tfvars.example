resource_group_name = "RG-FileServerLab" # case-sensitive - must match Lab 07 exactly
vm_name              = "FS01"             # case-sensitive - must match Lab 07 exactly

# Object IDs for your three test users.
# Run ../scripts/00-create-test-users.ps1 first - it creates the users
# AND prints these three lines ready to paste in.
sysadmin_object_id     = "REPLACE_WITH_SYSADMIN_OBJECT_ID"
support_user_object_id = "REPLACE_WITH_SUPPORTTECH_OBJECT_ID"
auditor_object_id      = "REPLACE_WITH_AUDITOR_OBJECT_ID"
